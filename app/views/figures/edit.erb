<form method="post" action="/figures/<%= @figure.id %>">
  <input type="hidden" name="_method" value="patch" />
  <input type="text" name="figure[name]" id="figure_name" value="<%= @figure.name %>" />

  <br />
  <% @landmarks.each do |landmark| %>
    <input type="checkbox" name="figure[landmark_ids][]" value="<%= landmark.id %>" <%= "checked" if landmark.figure == @figure %>> <%= landmark.name %>  </input>
  <% end %>

  Or create a new one:
  <input type="text" name="landmark[name]" id="new_landmark" placeholder="Landmark Name" />
  <br />

  <br />

  Choose from all titles:
  <% @titles.each do |title| %>
    <input type="checkbox" name="figure[title_ids][]" value="<%= title.id %>" <%="checked" if @figure.titles.include?(title)%> > <%= title.name %>  </input>
  <% end %>
  <br />
  Or create a new title:
  <input type="text" name="title[name]" id="new_title" placeholder="Title Name" />

<br />
  <input type="submit" value="Edit Figure" />
</form>
